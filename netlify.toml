[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# SPA fallback for deep links (/guest, /portal, /admin, etc.)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
